const initialState = {
  main: {
    result: [1, 2, 3],
  },
};

const mineReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default mineReducer;
